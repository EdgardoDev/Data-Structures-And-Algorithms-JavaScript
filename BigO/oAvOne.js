// Here we return 2 operations and no matter the number of operations performed
// It'll be a constant so this can be simplified to O(1), (Constant time).
// This is the most efficient Big O, O(1)

function addItems(n) {
    return n + n + n;
}